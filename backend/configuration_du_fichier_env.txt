# ──────────────────────────────────────────────
# SHIELD Backend — fichier .env
# Copiez ce fichier dans backend/.env et
# remplissez les valeurs avant de lancer l'API.
# ──────────────────────────────────────────────

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=signature_communiques_officiels
DB_USER=shield
DB_PASSWORD=securepassword

# JWT
JWT_SECRET_KEY=changez_cette_cle_en_production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=15

# Bcrypt
BCRYPT_ROUNDS=12

# OTP / 2FA
OTP_EXPIRE_SECONDS=300
OTP_LENGTH=6

# Clé Fernet pour chiffrer les clés privées RSA
# Générez avec : python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=votre_cle_fernet_generee

# Connexion a sont compte gmail de lapplication
MAIL_USERNAME=signaturenumerique.shield@gmail.com
MAIL_PASSWORD=kzie vmwk ivad bycz
MAIL_FROM=signaturenumerique.shield@gmail.com
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_TLS=True
MAIL_SSL=False

# Réinitialisation du mot de passe
FRONTEND_URL=http://localhost:5173
PASSWORD_RESET_EXPIRE_MINUTES=30